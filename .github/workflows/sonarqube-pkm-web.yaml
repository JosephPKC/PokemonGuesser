name: SonarQube
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    paths: 
      - 'PkmWeb/**'
  
jobs:
  sonarqube:
    uses: ./.github/workflows/sonarqube.yaml
    with:
      sln-file-path: ./PkmWeb/PkmWeb/PkmWeb.sln
    secrets: inherit