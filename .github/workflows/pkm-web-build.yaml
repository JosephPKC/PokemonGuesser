name: PkmWeb Build
run-name: Build for PR ( ${{ github.event.number }} ${{ github.event.pull_request.title }}) by ${{ github.actor }}
on:
  push:
    branches: [main]
    paths: ['PkmWeb/PkmWeb/**']
jobs:
  build-test:
    uses: ./.github/workflows/build-test.yaml
    with:
      sln-path: ./PkmWeb/PkmWeb.sln
  build-push-docker:
    needs: [build-test]
    uses: ./.github/workflows/build-docker.yaml
    with:
      docker-file-path: ./PkmWeb/PkmWeb/Dockerfile
      docker-tag-name: pkm-web
    secrets: inherit
  deploy-kubernetes:
    needs: [build-test, build-push-docker]
    uses: ./.github/workflows/deploy-kubernetes.yaml
